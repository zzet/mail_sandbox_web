%h1= t(".edit_profile")

= render 'web/shared/error_messages', errors:  @user.errors

= simple_form_for @user, url: account_profile_path do |f|
  = f.input :email
  = f.input :password
  = f.button :submit
